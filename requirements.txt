# POL Command Center — dependencias de ejecución (versiones fijadas)
# Ref: design §12.1
dash==2.18.*
dash-bootstrap-components==1.6.*
plotly==5.24.*
numpy==2.1.*
pandas==2.2.*

# Lectura de data/prices.yaml (modelo económico, sim/economics.py — design §10.1)
pyyaml==6.0.*

# Servidor WSGI de producción — un solo worker es obligatorio (design §3.3)
gunicorn==23.0.*

# Dependencias de prueba
pytest
hypothesis
